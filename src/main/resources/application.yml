spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.64.55.164:3306/goodoggy_db?userSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: webadmin
    password: Goodoggy2022!
    hikari.connection-test-query: select 1 from dual

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
      format-sql: true
      enable-lazy-load-no-trans: true
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

gcp:
  config:
    file: marine-resource-369813-bd9582cfbae3.json
  project:
    id: marine-resource-369813
  bucket:
    id: goodoggy_bucket

server:
  port: 8080
  servlet:
    session:
      timeout: 1800
      tracking-modes: cookie